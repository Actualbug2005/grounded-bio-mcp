# CRISPOR runtime dependencies — Python 3.10 only.
#
# Pinned to the exact versions CRISPOR's bundled Azimuth-2.0 and rs3
# ML scoring models were built against. Newer versions break the
# scorers in non-obvious ways at predict-time. See
# docs/crispor_install.md for the rationale, the install procedure,
# and the scripts/crispor_sitecustomize.py shim that complements
# this requirements file.
#
# DO NOT pass --only-binary=:all: when installing — twobitreader and
# pytabix are sdist-only on PyPI (pure Python and a small C extension;
# both source-build in seconds with build-essential available).

scikit-learn==1.0.2
numpy==1.22.4
scipy==1.8.1
biopython==1.79
pandas==1.4.2
matplotlib==3.5.2
twobitreader==3.1.7
pytabix==0.1
joblib==1.1.0
xlwt==1.3.0
six==1.16.0
lightgbm==3.3.5
rs3==0.0.15
lmdb==1.3.0
lmdbm==0.0.5
